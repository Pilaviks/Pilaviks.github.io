# Personal website

Static personal site — a single `index.html` with inline CSS/JS plus image assets.
No build step.

Deployed with GitHub Pages from the `main` branch.

## Local preview

```sh
python3 -m http.server 8000
# open http://localhost:8000
```
